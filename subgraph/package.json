{
  "name": "mev-patterns-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for MEV pattern indexing",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mev-patterns",
    "create-local": "graph create --node http://localhost:8020/ mev-patterns",
    "remove-local": "graph remove --node http://localhost:8020/ mev-patterns"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.56.0",
    "@graphprotocol/graph-ts": "0.31.0"
  }
}
